rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Qualquer pessoa com o link do site pode ler e escrever partidas.
    // Não há login: a "senha" de acesso é o próprio link do site.
    match /matches/{matchId} {
      allow read, write: if true;
    }
  }
}
